export const logoutUser = () => {
    //router.post("/logout");
}

export const loginUser = (credential, password) => {
    //router.post("/login", {
     //   'credential': credential,
    //    'password': password
    //});
}